---
description: Analyze consistency and coverage across spec, plan, and tasks.
agent: general
subtask: true
---

Load `specs-driven`. Analyze this feature without modifying implementation code:

$ARGUMENTS

Cross-check `spec.md`, `plan.md`, and `tasks.md` for ambiguity, contradictions, missing requirement coverage, missing security or data work, unverifiable criteria, unnecessary scope, and tasks without requirements. Report findings by severity with artifact references, then provide a coverage table for every requirement and acceptance criterion.
