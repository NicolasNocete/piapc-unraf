---
description: Generate traceable implementation tasks from a feature plan.
agent: build
---

Load `specs-driven`. Resolve this feature under `specs/`:

$ARGUMENTS

Read `spec.md` and `plan.md`, then create or update `tasks.md`. Order tasks by dependency, keep each task independently verifiable, reference requirement and acceptance-criterion IDs, and include explicit verification tasks. Do not implement code.
