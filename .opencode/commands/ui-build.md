---
description: Build accessible PIAPC UI with project-aware shadcn patterns.
agent: build
---

Load `shadcn`, `piapc-shadcn-conventions`, `project-react-best-practices`, and `vercel-composition-patterns`. Implement:

$ARGUMENTS

Inspect `components.json` and installed components first. Use shadcn docs and registry search before custom primitives, preserve the Base UI API, and verify responsive layout, keyboard interaction, focus, loading, empty, and error states. Run relevant checks when complete.
