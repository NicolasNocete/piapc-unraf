---
description: Create or update a PIAPC feature specification.
agent: build
---

Load `specs-driven`. For this request:

$ARGUMENTS

Find an existing matching feature or create the next `specs/NNN-feature-name/spec.md`. Define what and why, not the technical implementation. Include stable requirement and acceptance-criterion IDs, explicit out-of-scope items, security/data impact, and `[NEEDS CLARIFICATION]` markers. Leave the status as Draft unless the user explicitly approves it.
