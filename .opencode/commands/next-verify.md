---
description: Verify an affected Next.js route against the running app.
agent: build
---

Load `piapc-next-conventions` and `next-dev-loop`. Verify this route or behavior against the running development server:

$ARGUMENTS

Inspect runtime errors, compilation issues, browser behavior, console output, and relevant accessibility state. Apply minimal in-scope fixes and repeat verification until it passes or a real blocker remains.
