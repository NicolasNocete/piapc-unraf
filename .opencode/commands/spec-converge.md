---
description: Compare implementation against a spec and append remaining work.
agent: build
---

Load `spec-implementation`. Converge this feature:

$ARGUMENTS

Compare the current implementation and verification evidence with every requirement, task, and acceptance criterion. Fix only clear in-scope defects. Add remaining executable work to `tasks.md`, preserving completed history. Report passed, failed, blocked, and unverified criteria separately.
