---
description: Create the technical plan for a PIAPC feature spec.
agent: build
---

Load `specs-driven`. Resolve this feature under `specs/`:

$ARGUMENTS

Read its `spec.md`, inspect the current codebase, and create or update `plan.md`. Cover architecture, affected files, server/client boundaries, data and migrations, authorization/RLS, shadcn usage, rollout or rollback, and verification. Reference requirement IDs. Stop instead of planning around unresolved requirement ambiguity.
