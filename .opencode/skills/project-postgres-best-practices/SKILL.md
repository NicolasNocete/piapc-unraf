---
name: project-postgres-best-practices
description: Use before SQL, migrations, schema, RLS, grants, indexes, functions, or Postgres diagnostics in PIAPC to apply the project-pinned official Supabase guidance.
---

# Project-Pinned Postgres Best Practices

Before writing or changing database objects, read and follow `.agents/skills/supabase-postgres-best-practices/SKILL.md` and every task-relevant file it references. That directory is pinned by `skills-lock.json`; do not substitute a same-named global skill.

Also load `project-supabase` and `piapc-supabase-rules` for current product and project-specific security guidance.
