---
name: project-react-best-practices
description: Use when writing, reviewing, or optimizing React and Next.js UI in PIAPC to apply the official Vercel skill version pinned by this project.
---

# Project-Pinned React Best Practices

Before React or Next.js performance work, read and follow `.agents/skills/vercel-react-best-practices/SKILL.md` and its task-relevant references. That directory is the official Vercel skill pinned by `skills-lock.json`; do not substitute a same-named global skill.

Combine it with `piapc-next-conventions`, and use `vercel-composition-patterns` only when component API design or repeated composition makes it relevant.
